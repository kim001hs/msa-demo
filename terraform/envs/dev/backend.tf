# ==============================================================================
# S3 원격 백엔드 설정 (선택 사항)
# ==============================================================================
# 1. bootstrap 모듈을 먼저 배포한 후 생성된 S3 버킷 이름과 DynamoDB 테이블 이름을
#    아래에 기입하고 주석(#)을 해제하세요.
# 2. 그런 다음 `terraform init -migrate-state` 명령어를 실행하면 로컬 상태 파일이
#    S3 원격 저장소로 안전하게 마이그레이션됩니다.
# ==============================================================================

# terraform {
#   backend "s3" {
#     bucket         = "<bootstrap에서_출력된_S3_버킷_이름>"
#     key            = "envs/dev/terraform.tfstate"
#     region         = "ap-northeast-2"
#     dynamodb_table = "msa-demo-tflock"
#     encrypt        = true
#   }
# }

